jenkins:
  # $JENKINS_URL
  ## url: "https://10.10.10.10:8080"
  view: "default"
  authentication:
    enabled: True
    # $JENKINS_USER
    ## user: "jenkins_user"

    # $JENKINS_PASSWORD
    ## password: "jenkins_passowrd"

    # $JENKINS_CRUMB
    ## crumb: "Jenkins-Crumb:e3d49c085347bf2cc24aca382f698a70"

parameters:
  # Name of the stack to test, caasp|microos
  stack: "caasp"

  # IMAGE parameter
  image: True

  # IMAGE_URL parameter
  image_url: True

jobs:
  hyperv:
    - name: "hyperv-push-image"
      parameters:
        toto: "help"

  #openstack:
  #  - name: "openstack-push-image"
  #
  #kvm:
  #  - name: "kvm-push-image"

  vmware:
    - name: "vmware-push-image"

  #xen:
  #  - name: "xen-push-image"
